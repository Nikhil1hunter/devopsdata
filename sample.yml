---
- hosts: all
  become: yes
  tasks:
    - name: Update apt package index
      apt:
        update_cache: yes

    - name: Install openjdk-8
      apt:
        name: openjdk-8-jre
        state: present