---
- hosts: all
  become: yes
  tasks:
       - name: i want install tree
         apt:
            name: tree
            state: present
       - name: Install apache2
         apt:
           name: apache2
           state: absent
       - name: start apache service
         service:
            name: apache2
            state: started
